#include  <math.h>
int main()
{
int x;
    sin(x);
    cos(x);
    tan(x); //Data type double
    log(X);
    pow(x,y); // ---> double

return 0;
}
#include <stdlib.h> //>> rand(); srand(); sizeof();
#include