/*What happens if you try assign a pointer any address e.g F176BA2
a)Will the compiler allow you?
b)If so, print the contents of that address and see what happens. 
Which delimiter should you use?
c)Write a short program to declare an integer and a pointer. 
Make the pointer point to the integer.
Input an integer value into the int variable. 
Using the indirection operator, 
output the contents of address stored in the pointer.
d)Now, increment the pointer (e.g. ptr++) and output the contents of the 
new address stored in the pointer. What do you see?
e)Can you assign any value to a randomaddress (e.g. using the indirection operator,
can you assign the value 10 into the address FB6546)? Try
Author: Adlane
Date: 23/11/2019 */

#include <stdio.h>
int main()
{
    

    return 0;
}